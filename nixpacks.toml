[phases.setup]
nixPkgs = ["jdk", "maven"]

[phases.build]
cmds = ["cd backend && mvn package -DskipTests -q"]

[start]
cmd = "java -Dspring.profiles.active=prod -jar backend/target/webbh-sistema-1.0.0.jar"
